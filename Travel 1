<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="google-site-verification" content="Ck-24AGvEDrzbbaaQAFbJKvtXCeFJHs_MHyeK7zBKbQ" />
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gurukul Kangri (GKDU) Fresher's Guide</title>
    <meta name="description" content="The ultimate unofficial survival and resource guide for Gurukul Kangri (GKDU) freshers.">
    <meta name="keywords" content="Gurukul Kangri, GKDU, Haridwar, GKV freshers guide, GKDU admission">

    <style>
        @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;700&family=Bebas+Neue&display=swap');

        :root {
            --bg-color: #f4f4f0;
            --bg-dots: #000000;
            --container-bg: #ffffff;
            --text-main: #000000;
            --text-muted: #333333;
            --border-color: #000000;
            --card-bg: #ffffff;
            
            --color-yellow: #FFDE00;
            --color-green: #00E676;
            --color-pink: #FF90E8;
            --color-blue: #90E0EF;
            --color-purple: #CDB4DB;
            --color-orange: #FF6B6B;
            --btn-text: #000000;
        }

        [data-theme="dark"] {
            --bg-color: #121212;
            --bg-dots: #444444;
            --container-bg: #1a1a1a;
            --text-main: #ffffff;
            --text-muted: #dddddd;
            --border-color: #ffffff;
            --card-bg: #222222;
        }

        /* --- CONTINUOUS KEYFRAMES --- */
        @keyframes panBackground {
            0% { background-position: 0 0; }
            100% { background-position: 40px 40px; }
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(-1deg); }
            50% { transform: translateY(-15px) rotate(2deg); }
        }

        @keyframes floatReverse {
            0%, 100% { transform: translateY(0px) rotate(1deg); }
            50% { transform: translateY(15px) rotate(-2deg); }
        }

        @keyframes wiggle {
            0%, 100% { transform: rotate(0deg); }
            25% { transform: rotate(3deg); }
            75% { transform: rotate(-3deg); }
        }

        @keyframes rainbowShadow {
            0% { box-shadow: 12px 12px 0px var(--color-yellow); }
            25% { box-shadow: 12px 12px 0px var(--color-pink); }
            50% { box-shadow: 12px 12px 0px var(--color-blue); }
            75% { box-shadow: 12px 12px 0px var(--color-green); }
            100% { box-shadow: 12px 12px 0px var(--color-yellow); }
        }

        @keyframes marquee {
            0% { transform: translateX(100%); }
            100% { transform: translateX(-100%); }
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        @keyframes pulseAlert {
            0% { transform: scale(1); box-shadow: 0 0 0 0 rgba(255, 107, 107, 0.7); }
            70% { transform: scale(1.05); box-shadow: 0 0 0 15px rgba(255, 107, 107, 0); }
            100% { transform: scale(1); box-shadow: 0 0 0 0 rgba(255, 107, 107, 0); }
        }

        @keyframes jelly {
            0% { transform: scale(1, 1); }
            25% { transform: scale(0.9, 1.1); }
            50% { transform: scale(1.1, 0.9); }
            75% { transform: scale(0.95, 1.05); }
            100% { transform: scale(1, 1); }
        }

        /* --- SCROLL REVEAL KEYFRAMES --- */
        @keyframes slideInLeft {
            from { opacity: 0; transform: translateX(-100px) rotate(-5deg); }
            to { opacity: 1; transform: translateX(0) rotate(0); }
        }
        @keyframes slideInRight {
            from { opacity: 0; transform: translateX(100px) rotate(5deg); }
            to { opacity: 1; transform: translateX(0) rotate(0); }
        }
        @keyframes popIn {
            from { opacity: 0; transform: scale(0.5) translateY(50px); }
            to { opacity: 1; transform: scale(1) translateY(0); }
        }

        body {
            font-family: 'Space Grotesk', sans-serif;
            background-color: var(--bg-color);
            background-image: radial-gradient(var(--bg-dots) 2px, transparent 2px);
            background-size: 30px 30px;
            color: var(--text-main);
            margin: 0;
            padding: 40px 20px;
            display: flex;
            justify-content: center;
            line-height: 1.6;
            transition: background-color 0.5s ease, color 0.5s ease;
            overflow-x: hidden;
            animation: panBackground 4s linear infinite;
        }

        /* Animated Marquee Strip */
        .ticker-tape {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background: var(--color-orange);
            color: #000;
            font-weight: 900;
            font-size: 1.2em;
            padding: 5px 0;
            border-bottom: 4px solid var(--border-color);
            z-index: 1000;
            overflow: hidden;
            white-space: nowrap;
        }
        .ticker-tape p {
            display: inline-block;
            margin: 0;
            padding-left: 100%;
            animation: marquee 15s linear infinite;
        }

        .container {
            max-width: 800px;
            width: 100%;
            background: var(--container-bg);
            padding: 40px;
            border-radius: 12px;
            border: 4px solid var(--border-color);
            position: relative;
            margin-top: 30px;
            /* Constant animated shadow */
            animation: rainbowShadow 6s infinite linear;
            transition: transform 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .container:hover {
            transform: scale(1.01);
        }

        /* --- Scroll Reveal Classes --- */
        .reveal-left { opacity: 0; }
        .reveal-left.active { animation: slideInLeft 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards; }
        
        .reveal-right { opacity: 0; }
        .reveal-right.active { animation: slideInRight 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards; }
        
        .reveal-pop { opacity: 0; }
        .reveal-pop.active { animation: popIn 0.7s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards; }

        /* --- Theme Toggle Button --- */
        .theme-toggle {
            position: absolute;
            top: 20px;
            right: 20px;
            background: var(--container-bg);
            border: 3px solid var(--border-color);
            color: var(--text-main);
            padding: 8px 15px;
            border-radius: 50px;
            font-family: 'Space Grotesk', sans-serif;
            font-weight: 900;
            cursor: pointer;
            box-shadow: 4px 4px 0px var(--border-color);
            transition: all 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            z-index: 10;
        }

        .theme-toggle:hover {
            transform: translate(-2px, -4px) scale(1.1) rotate(-5deg);
            box-shadow: 8px 8px 0px var(--border-color);
            background: var(--color-yellow);
            color: #000;
        }
        .theme-toggle:active {
            transform: translate(4px, 4px) scale(0.9);
            box-shadow: 0px 0px 0px var(--border-color);
        }
        .theme-icon { display: inline-block; transition: transform 0.5s; }
        .theme-toggle:hover .theme-icon { animation: spin 1s linear infinite; }

        .header {
            text-align: center;
            margin-bottom: 40px;
            border-bottom: 6px solid var(--border-color);
            background: var(--color-yellow);
            color: #000;
            margin: -40px -40px 40px -40px;
            padding: 60px 20px 30px 20px;
            border-radius: 8px 8px 0 0;
            position: relative;
            overflow: hidden;
            animation: float 4s ease-in-out infinite;
        }

        .header::before {
            content: "🚀";
            position: absolute;
            font-size: 5em;
            opacity: 0.2;
            top: -10px;
            left: 20px;
            animation: floatReverse 3s infinite ease-in-out;
        }
        .header::after {
            content: "🎓";
            position: absolute;
            font-size: 5em;
            opacity: 0.2;
            bottom: -10px;
            right: 20px;
            animation: float 2.5s infinite ease-in-out;
        }

        .header h1 {
            font-family: 'Bebas Neue', sans-serif;
            font-size: 4.5em;
            margin: 0 0 5px 0;
            letter-spacing: 2px;
            text-shadow: 4px 4px 0px #fff, 8px 8px 0px var(--border-color);
            transition: transform 0.3s;
        }
        .header h1:hover {
            animation: jelly 0.8s ease;
        }

        .header p {
            font-size: 1.4em;
            font-weight: 900;
            margin: 0;
            background: #000;
            color: #fff;
            display: inline-block;
            padding: 5px 15px;
            transform: rotate(-2deg);
            border: 2px solid #fff;
            box-shadow: 4px 4px 0 #fff;
        }

        .section { margin-bottom: 60px; }

        .section-title {
            font-size: 1.8em;
            text-transform: uppercase;
            margin-bottom: 25px;
            display: inline-block;
            background: var(--color-blue);
            color: #000;
            padding: 8px 20px;
            border: 4px solid var(--border-color);
            box-shadow: 6px 6px 0px var(--border-color);
            transform: rotate(-2deg);
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            z-index: 2;
        }
        .section-title:hover {
            transform: rotate(3deg) scale(1.1);
            background: var(--color-pink);
            box-shadow: -6px 6px 0px var(--border-color);
        }

        .grid-2 {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .card {
            background: var(--card-bg);
            padding: 30px;
            border-radius: 0px;
            border: 4px solid var(--border-color);
            box-shadow: 8px 8px 0px var(--border-color);
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
        }

        .card:hover {
            transform: translate(-5px, -5px) rotate(1deg);
            box-shadow: 15px 15px 0px var(--border-color);
            background: var(--bg-color); /* Slight bg shift */
        }

        .card h3 {
            margin: 0 0 25px 0;
            font-size: 1.5em;
            text-transform: uppercase;
            border-bottom: 4px solid var(--border-color);
            padding-bottom: 10px;
            display: inline-block;
        }

        /* --- Crazy Button Styling --- */
        .btn-group {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
        }

        .btn {
            display: inline-flex;
            justify-content: center;
            align-items: center;
            gap: 8px;
            background: var(--color-pink);
            color: var(--btn-text) !important;
            padding: 12px 20px;
            text-decoration: none;
            border-radius: 0;
            font-size: 1.1em;
            font-weight: 900;
            text-align: center;
            border: 4px solid var(--border-color);
            box-shadow: 6px 6px 0px var(--border-color);
            transition: all 0.1s;
            flex: 1 1 auto;
            text-transform: uppercase;
            position: relative;
            overflow: hidden;
        }

        .btn::after {
            content: '';
            position: absolute;
            top: 0; left: -100%;
            width: 100%; height: 100%;
            background: rgba(255,255,255,0.3);
            transition: transform 0.4s;
        }

        .btn:hover::after {
            transform: translateX(200%);
        }

        .btn:hover {
            transform: translate(-3px, -3px);
            box-shadow: 10px 10px 0px var(--border-color);
            animation: wiggle 0.4s ease-in-out;
        }

        .btn:active {
            transform: translate(6px, 6px);
            box-shadow: 0px 0px 0px var(--border-color);
        }

        .btn-secondary { background: var(--color-green); }
        .btn-tertiary { background: var(--color-blue); }
        .btn-quaternary { background: var(--color-yellow); }

        .btn-icon { font-size: 1.3em; transition: transform 0.3s; }
        .btn:hover .btn-icon { transform: scale(1.4) rotate(15deg); }

        .link-list { list-style: none; padding: 0; margin: 0; }
        .link-list li { margin-bottom: 15px; }

        .link-list a {
            text-decoration: none;
            color: var(--text-main);
            font-weight: 900;
            display: flex;
            justify-content: space-between;
            padding: 12px 15px;
            border: 4px solid var(--border-color);
            box-shadow: 4px 4px 0px var(--border-color);
            background: var(--card-bg);
            transition: all 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }
        
        .link-list a:hover {
            background: var(--color-yellow);
            color: #000;
            transform: translateX(10px) translateY(-5px);
            box-shadow: -4px 8px 0px var(--border-color);
        }

        .link-list a span { font-size: 1.2em; font-weight: 900; transition: transform 0.3s;}
        .link-list a:hover span { transform: translateX(5px); }

        .tip-box {
            background: var(--card-bg);
            border: 4px solid var(--border-color);
            padding: 25px;
            margin-top: 30px;
            font-size: 1.1em;
            font-weight: 700;
            transition: all 0.3s ease;
            position: relative;
        }

        .tip-box::before {
            content: '';
            position: absolute;
            top: -15px; left: -15px;
            width: 30px; height: 30px;
            background: var(--color-orange);
            border: 4px solid var(--border-color);
            border-radius: 50%;
            animation: pulseAlert 2s infinite;
        }

        .tip-box:hover {
            transform: scale(1.03) rotate(-1deg);
            box-shadow: 10px 10px 0px var(--border-color);
            background: var(--color-purple);
            color: #000;
        }

        .tip-box strong {
            background: #000;
            color: var(--color-yellow);
            padding: 4px 8px;
            border: 2px solid var(--border-color);
            margin-right: 5px;
            display: inline-block;
            transform: rotate(-3deg);
        }

        /* --- Advanced FAQ Section --- */
        .faq-search-box {
            width: 100%;
            padding: 18px 20px;
            font-family: 'Space Grotesk', sans-serif;
            font-size: 1.2em;
            font-weight: 900;
            border: 4px solid var(--border-color);
            margin-bottom: 30px;
            background: var(--card-bg);
            color: var(--text-main);
            box-sizing: border-box;
            box-shadow: 6px 6px 0px var(--border-color);
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }
        
        .faq-search-box:focus {
            outline: none;
            background: var(--color-green);
            color: #000;
            transform: scale(1.02) translateY(-5px);
            box-shadow: 10px 10px 0px var(--border-color);
        }

        .faq-container { display: flex; flex-direction: column; gap: 20px; }
        
        details {
            background: var(--card-bg);
            border: 4px solid var(--border-color);
            box-shadow: 6px 6px 0px var(--border-color);
            overflow: hidden;
            transition: all 0.3s ease;
        }
        
        details[open] {
            background: var(--color-yellow);
            color: #000;
            animation: jelly 0.4s ease;
            box-shadow: 10px 10px 0px var(--border-color);
            transform: translateX(10px);
        }
        
        summary {
            font-size: 1.2em;
            font-weight: 900;
            padding: 20px;
            cursor: pointer;
            list-style: none;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        summary::-webkit-details-marker { display: none; }
        summary::after { 
            content: '👇'; 
            font-size: 1.5em; 
            transition: transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275); 
        }
        details[open] summary::after { transform: rotate(180deg) scale(1.2); }
        
        .faq-content {
            padding: 0 20px 20px 20px;
            font-weight: 700;
            font-size: 1.05em;
            border-top: 4px dashed var(--border-color);
            margin-top: 10px;
            padding-top: 20px;
            background: var(--card-bg); 
            color: var(--text-main);
            animation: slideInLeft 0.4s ease;
        }

        /* --- Review Section --- */
        .review-section {
            background: var(--color-purple);
            color: #000;
            padding: 50px 30px;
            text-align: center;
            border: 6px solid var(--border-color);
            box-shadow: 12px 12px 0px var(--border-color);
            margin-top: 70px;
            transform: rotate(2deg);
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            animation: float 5s infinite;
        }

        .review-section:hover {
            transform: rotate(-2deg) scale(1.05);
            background: var(--color-orange);
            box-shadow: -12px 12px 0px var(--border-color);
        }

        .review-section h2 { margin-top: 0; font-size: 2.5em; text-transform: uppercase; font-family: 'Bebas Neue', sans-serif; letter-spacing: 2px;}
        .review-section p { font-weight: 900; font-size: 1.3em; margin-bottom: 30px; }

        .action-button {
            display: inline-block;
            background: var(--color-yellow);
            color: #000 !important;
            text-decoration: none;
            padding: 20px 40px;
            font-weight: 900;
            font-size: 1.5em;
            text-transform: uppercase;
            text-align: center;
            border: 5px solid var(--border-color);
            box-shadow: 8px 8px 0px var(--border-color);
            transition: all 0.1s; 
        }

        .action-button:hover {
            transform: translate(-4px, -4px);
            box-shadow: 12px 12px 0px var(--border-color);
            background: var(--color-green);
            animation: wiggle 0.3s infinite;
        }
        .action-button:active {
            transform: translate(8px, 8px);
            box-shadow: 0px 0px 0px var(--border-color);
        }

        .footer {
            text-align: center;
            margin-top: 60px;
            font-weight: 900;
            font-size: 1.2em;
            border-top: 6px dashed var(--border-color);
            padding-top: 30px;
        }

        .footer span {
            display: inline-block;
            color: #FF2A2A; 
            animation: floatReverse 1.5s infinite ease-in-out;
            font-size: 1.5em;
        }

        /* --- Floating Share Button --- */
        .floating-share {
            position: fixed;
            bottom: 30px;
            right: 30px;
            background: var(--color-blue);
            color: #000;
            border: 4px solid var(--border-color);
            box-shadow: 6px 6px 0px var(--border-color);
            border-radius: 0;
            padding: 15px 30px;
            font-size: 1.2em;
            font-weight: 900;
            font-family: 'Space Grotesk', sans-serif;
            cursor: pointer;
            z-index: 1000;
            transition: all 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            text-transform: uppercase;
            animation: pulseAlert 3s infinite;
        }
        
        .floating-share:hover {
            transform: translate(-5px, -5px) scale(1.1) rotate(-3deg);
            box-shadow: 12px 12px 0px var(--border-color);
            background: var(--color-pink);
            animation: none; /* stop pulse on hover */
        }
        .floating-share:active {
            transform: translate(6px, 6px);
            box-shadow: 0px 0px 0px var(--border-color);
        }

        /* Fun interaction element */
        .cursor-trail {
            position: fixed;
            width: 20px; height: 20px;
            border: 3px solid var(--border-color);
            background: var(--color-yellow);
            border-radius: 50%;
            pointer-events: none;
            z-index: 9999;
            transition: transform 0.1s;
            mix-blend-mode: difference;
            display: none; /* Enabled via JS on desktop */
        }

        @media (max-width: 600px) {
            body { padding: 40px 10px 20px 10px; }
            .ticker-tape { font-size: 1em; padding: 3px 0;}
            .container { padding: 25px 15px; border-width: 4px; box-shadow: 8px 8px 0px var(--border-color); margin-top: 10px;}
            .header { margin: -25px -15px 30px -15px; padding: 60px 10px 20px 10px; }
            .header h1 { font-size: 3em; }
            .section-title { font-size: 1.4em; }
            .action-button { width: 100%; box-sizing: border-box; }
            .floating-share { bottom: 20px; right: 20px; padding: 12px 20px; font-size: 1em; }
            .theme-toggle { top: -25px; right: 10px; font-size: 0.85em; padding: 6px 12px;}
        }
    </style>
</head>
<body>

    <!-- Ticker Tape -->
    <div class="ticker-tape">
        <p>🔥 WELCOME FRESHERS! 🔥 SURVIVE GKDU 🔥 JOIN THE GROUPS 🔥 GET PREVIOUS YEAR PAPERS 🔥 STUDY HARD 🔥 PARTY HARD 🔥</p>
    </div>

    <div class="cursor-trail" id="cursor"></div>

    <div class="container reveal-pop active">
        
        <button class="theme-toggle" id="themeBtn" onclick="toggleTheme()">
            <span class="theme-icon">🌙</span> Dark
        </button>

        <div class="header">
            <h1>Gurukul Kangri</h1>
            <p>THE UNOFFICIAL GUIDE</p>
        </div>

        <div class="section">
            <h2 class="section-title reveal-left">🏛️ Portals & Services</h2>
            <div class="grid-2">
                <div class="card reveal-pop">
                    <h3>General Portals</h3>
                    <div class="btn-group" style="flex-direction: column;">
                        <a href="https://share.google/vnUCMSDR4Nt3ovtIa" class="btn" target="_blank"><span class="btn-icon">🌐</span> Main Website</a>
                        <a href="https://share.google/mQrSIhndp8oXtUgdC" class="btn btn-secondary" target="_blank"><span class="btn-icon">⚡</span> Alternate Portal</a>
                    </div>
                </div>
                <div class="card reveal-right">
                    <h3>Student Services</h3>
                    <div class="btn-group" style="flex-direction: column;">
                        <a href="https://share.google/f4ZzQM094yr221HPa" class="btn btn-tertiary" target="_blank"><span class="btn-icon">💰</span> Download Fee Receipt</a>
                        <a href="https://share.google/dKpY3ByxepoVHzmUV" class="btn btn-quaternary" target="_blank"><span class="btn-icon">💯</span> Check Results</a>
                    </div>
                </div>
            </div>
        </div>

        <div class="section">
            <h2 class="section-title reveal-right">📱 Campus Community</h2>
            <div class="grid-2">
                <div class="card reveal-left">
                    <h3>Chat Groups</h3>
                    <div class="btn-group" style="flex-direction: column;">
                        <a href="https://ig.me/j/AbYV1fHlJ03KWBBm/" class="btn" target="_blank"><span class="btn-icon">👨🏽</span> Boys Discussion</a>
                        <a href="https://ig.me/j/AbYq82f0ZOMoNNI3/" class="btn btn-tertiary" target="_blank"><span class="btn-icon">👩🏽</span> Girls Discussion</a>
                    </div>
                </div>
                <div class="card reveal-pop">
                    <h3>Insta Directory</h3>
                    <ul class="link-list">
                        <li><a href="https://www.instagram.com/gkv_hdr?igsh=MTk0ZGprc3I2dWtrYQ==" target="_blank">Official GKV Page <span>↗</span></a></li>
                        <li><a href="https://www.instagram.com/gkviians?igsh=MjJjemZsaGZ3MzN5" target="_blank">@gkviians (Memes) <span>↗</span></a></li>
                        <li><a href="https://www.instagram.com/na_samaz_bandar?igsh=MTh4bngxaTRzcnc5YQ==" target="_blank">@na_samaz_bandar <span>↗</span></a></li>
                        <li><a href="https://www.instagram.com/lekhram_diaries?igsh=cGI0amozZXNzb3Ru" target="_blank">@lekhram_diaries <span>↗</span></a></li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="section">
            <h2 class="section-title reveal-left">📚 Academic Resources</h2>
            <div class="card reveal-pop">
                <div class="btn-group" style="flex-direction: column;">
                    <a href="https://drive.google.com/drive/mobile/folders/1nVBWVYDDvre_38XRq506y6ii7-zo_XT6?fbclid=PAT01DUATq69FwZG9mAmV4dG4DYWVtAjEwAHNydGMGYXBwX2lkDzU2NzA2NzM0MzM1MjQyNwABp4gyXM6uuy0MKePYEZRO-ErmfkhkNwGhJSHX6svjKgQfqNAwKKPBEt9qq8gv_aem_P1S3L95_kZj3q1tY1_MxcQ" class="btn btn-quaternary" target="_blank"><span class="btn-icon">📂</span> Download All Papers</a>
                    <a href="https://share.google/G90iNCkTZpMHpG3pT" class="btn btn-secondary" target="_blank"><span class="btn-icon">📝</span> FET Previous Papers</a>
                </div>
                <div class="tip-box">
                    <strong>💡 PRO TIP:</strong> The first semester sets your trajectory. Securing a high SGPA early on provides a massive advantage. Focus heavily on mastering core subjects!
                </div>
            </div>
        </div>

        <div class="section">
            <h2 class="section-title reveal-right">🏢 Campus & Hostel</h2>
            <div class="card reveal-left">
                <div class="btn-group">
                    <a href="https://maps.app.goo.gl/mDGiPte6tJTpeBtB7" class="btn" target="_blank"><span class="btn-icon">🏠</span> Hostel Images</a>
                    <a href="https://maps.app.goo.gl/F7QrGuoJQ2ATgpur8" class="btn" style="background: var(--color-purple);" target="_blank"><span class="btn-icon">🏫</span> Campus Images</a>
                </div>
                <div class="tip-box">
                    <strong>☢️ SURVIVAL TIP:</strong> Be self-reliant! Campus amenities can be unpredictable. Highly recommend bringing your own dry snacks and basic utensils.
                </div>
            </div>
        </div>

        <div class="section">
            <h2 class="section-title reveal-pop">💬 Fresher FAQs</h2>
            
            <input type="text" id="faqSearch" class="faq-search-box reveal-left" placeholder="🔍 TYPE HERE TO SEARCH FAQS..." onkeyup="filterFAQs()">

            <div class="faq-container" id="faqContainer">
                
                <details class="faq-item reveal-right">
                    <summary>Are laptops mandatory for 1st-year?</summary>
                    <div class="faq-content">
                        While not strictly mandatory for day one, you'll need one for coding assignments and projects by the middle of the first semester. A decent mid-range laptop is enough!
                    </div>
                </details>
                
                <details class="faq-item reveal-left">
                    <summary>Is a hostel room guaranteed for freshers?</summary>
                    <div class="faq-content">
                        Not automatically. Hostel seats are limited and allotted strictly on a <strong>first-come, first-served</strong> basis. Generally, 1st-year students and a few 2nd-year students get accommodation. Apply as early as possible!
                    </div>
                </details>

                <details class="faq-item reveal-right">
                    <summary>Honest review: How are the hostel facilities?</summary>
                    <div class="faq-content">
                        To be completely transparent, it's a basic survival experience. Students occasionally face issues with water supply, sanitation, and average mess food. We highly recommend being self-reliant and keeping dry snacks handy!
                    </div>
                </details>

                <details class="faq-item reveal-left">
                    <summary>How are the classrooms and practical labs?</summary>
                    <div class="faq-content">
                        The engineering building infrastructure is decent. Practical labs and workshops are average—they meet all your basic academic requirements and get the job done, but don't expect ultra-modern tech parks.
                    </div>
                </details>

                <details class="faq-item reveal-right">
                    <summary>How are the professors and teaching staff?</summary>
                    <div class="faq-content">
                        Most faculty members are very knowledgeable and supportive! Each branch typically has a staff strength of 6–7 teachers (around 2-3 permanent faculty, and the rest are contractual). Don't hesitate to reach out to them for help.
                    </div>
                </details>

            </div>
        </div>

        <div class="review-section reveal-pop">
            <h2>⭐ Help Future Freshers!</h2>
            <p>Your experience can guide the next batch.</p>
            <a href="https://forms.gle/s2w2vbcMQtyx6h8x5" class="action-button" target="_blank">✍️ Drop a Review</a>
        </div>

        <div class="footer reveal-pop">
            <p>Crafted with <span>❤️</span> by your senior</p>
        </div>

    </div>

    <button class="floating-share" id="shareButton" onclick="shareWebsite()">
        📢 Share Guide
    </button>

    <script>
        // 1. Theme Toggle Logic (Updated for GitHub/Local Storage safety)
        function toggleTheme() {
            const body = document.documentElement;
            const btn = document.getElementById('themeBtn');
            const icon = btn.querySelector('.theme-icon');
            
            // Add a fun shake animation to the whole container on toggle
            const container = document.querySelector('.container');
            container.style.animation = 'none';
            setTimeout(() => { container.style.animation = 'jelly 0.5s ease'; }, 10);
            setTimeout(() => { container.style.animation = 'rainbowShadow 6s infinite linear'; }, 510);

            try {
                if (body.getAttribute('data-theme') === 'dark') {
                    body.removeAttribute('data-theme');
                    localStorage.setItem('theme', 'light');
                    btn.innerHTML = '<span class="theme-icon">🌙</span> Dark';
                } else {
                    body.setAttribute('data-theme', 'dark');
                    localStorage.setItem('theme', 'dark');
                    btn.innerHTML = '<span class="theme-icon">☀️</span> Light';
                }
            } catch (e) {
                console.warn("Local storage is restricted. Theme won't save across reloads.", e);
                // Still toggle visually even if it won't save to storage
                if (body.getAttribute('data-theme') === 'dark') {
                    body.removeAttribute('data-theme');
                    btn.innerHTML = '<span class="theme-icon">🌙</span> Dark';
                } else {
                    body.setAttribute('data-theme', 'dark');
                    btn.innerHTML = '<span class="theme-icon">☀️</span> Light';
                }
            }
        }

        // Safely check local storage on load
        try {
            if (localStorage.getItem('theme') === 'dark') {
                document.documentElement.setAttribute('data-theme', 'dark');
                document.getElementById('themeBtn').innerHTML = '<span class="theme-icon">☀️</span> Light';
            }
        } catch(e) {
            console.warn("Local storage read restricted.");
        }

        // 2. Live Search Logic with Bouncy animations
        function filterFAQs() {
            let input = document.getElementById('faqSearch').value.toLowerCase();
            let details = document.querySelectorAll('.faq-item');

            details.forEach((item) => {
                let text = item.innerText.toLowerCase();
                if (text.includes(input)) {
                    if (item.style.display === "none") {
                        item.style.display = "";
                        item.style.animation = 'popIn 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275)';
                    }
                } else {
                    item.style.display = "none";
                }
            });
        }

        // 3. Share button (Updated for GitHub Pages/Clipboard safety)
        function shareWebsite() {
            const shareData = {
                title: "GKDU Fresher's Guide 🚀",
                text: "Check out this ultimate unofficial survival guide for Gurukul Kangri freshers!",
                url: window.location.href
            };

            const btn = document.getElementById('shareButton');
            btn.style.animation = 'jelly 0.5s ease';
            
            // Try native mobile sharing first
            if (navigator.share) {
                navigator.share(shareData).catch((error) => console.log('Error sharing:', error));
            } 
            // Fallback to clipboard if available
            else if (navigator.clipboard) {
                navigator.clipboard.writeText(window.location.href).then(() => {
                    const originalText = btn.innerHTML;
                    
                    btn.innerHTML = "✅ COPIED!";
                    btn.style.background = "var(--color-green)";
                    btn.style.transform = "scale(1.2) rotate(5deg)";
                    
                    setTimeout(() => {
                        btn.innerHTML = originalText;
                        btn.style.background = "var(--color-blue)";
                        btn.style.transform = "none";
                        btn.style.animation = 'pulseAlert 3s infinite';
                    }, 2000);
                }).catch(err => console.error('Failed to copy text: ', err));
            } 
            // Final fallback
            else {
                alert("Link copying is blocked by your browser settings. You can copy the URL directly from the address bar!");
            }
        }

        // 4. Advanced Scroll Reveal Observer (Multi-directional)
        document.addEventListener("DOMContentLoaded", function() {
            const revealElements = document.querySelectorAll(".reveal-left, .reveal-right, .reveal-pop");

            const revealObserver = new IntersectionObserver((entries, observer) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add("active");
                        observer.unobserve(entry.target); 
                    }
                });
            }, {
                root: null,
                threshold: 0.1, 
                rootMargin: "0px 0px -50px 0px"
            });

            revealElements.forEach(reveal => {
                revealObserver.observe(reveal);
            });
        });

        // 5. Custom Fun Cursor (Desktop only)
        if (window.matchMedia("(pointer: fine)").matches) {
            const cursor = document.getElementById('cursor');
            cursor.style.display = 'block';
            
            document.addEventListener('mousemove', e => {
                // Smooth follow
                setTimeout(() => {
                    cursor.style.left = e.clientX - 10 + 'px';
                    cursor.style.top = e.clientY - 10 + 'px';
                }, 50);
            });

            document.addEventListener('mousedown', () => {
                cursor.style.transform = 'scale(2)';
                cursor.style.background = 'var(--color-pink)';
            });

            document.addEventListener('mouseup', () => {
                cursor.style.transform = 'scale(1)';
                cursor.style.background = 'var(--color-yellow)';
            });
        }
    </script>
</body>
</html>
